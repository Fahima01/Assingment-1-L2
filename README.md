What I have done in this assignment:
1. Defined a function to filter even numbers from an array.
2. Created a function to reverse a string using built-in array methods.
3. Defined a union type and implemented a type guard function to distinguish between strings and numbers.
4. Designed an interface for a Book object and added a function to toggle a read status by extending the object.
5. Created a base Person class and a Student subclass, adding a grade property and a method to get details.
6. Wrote a generic function using type constraints to extract a property from an object safely.
7. Developed a function to find the intersection of two arrays by filtering common elements.
8. Used only return statements (no console.log) for all functions, ensuring modular and reusable code.
